{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "bower": "^1.7.9",
    "pulp": "^10.0.0",
    "rimraf": "^2.4.1"
  }
}
